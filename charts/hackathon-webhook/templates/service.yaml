apiVersion: v1
kind: Service
metadata:
  name: {{ include "hackathon-webhook.fullname" . }}
  labels:
    {{- include "hackathon-webhook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "hackathon-webhook.selectorLabels" . | nindent 4 }}
